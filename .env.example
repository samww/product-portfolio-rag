# Required
OPENAI_API_KEY=sk-...

# Optional — defaults shown
CHROMA_PERSIST_PATH=./chroma_data
APP_PORT=8000

# Azure AD (deployed environment only — not needed for local Docker)
# Run scripts/setup_auth.sh after first deployment to generate these values.
# AZURE_AD_TENANT_ID=
# AZURE_AD_CLIENT_ID=
# AZURE_AD_CLIENT_SECRET=

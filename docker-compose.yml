services:
  app:
    build:
      context: .
      args:
        OPENAI_API_KEY: ${OPENAI_API_KEY}
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - chroma_data:/app/.chroma

volumes:
  chroma_data:
